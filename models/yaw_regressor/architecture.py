"""
Архитектура кастомного CNN yaw-регрессора.

Вход: кроп детали из исходного кадра, resize до 64×64 или 128×128 (B, 3, H, W)
Выход: yaw угол в градусах (B, 1)

Архитектура: лёгкий CNN backbone + regression head
Написана с нуля на PyTorch (torch.nn.Module), без pretrained весов.

TODO: реализовать в Фазе 3.
"""
